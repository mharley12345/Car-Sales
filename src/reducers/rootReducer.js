  
const initialState = {
    additionalPrice: 0,
  };
  
  export const myDamnLocalReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };